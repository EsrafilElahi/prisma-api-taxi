const createDriver = require("./createDriver");
const { getDriver } = require("./getDriver");
const { getDrivers } = require("./getDriver");
const deleteDriver = require("./deleteDriver");

module.exports = { createDriver, getDriver, getDrivers, deleteDriver }